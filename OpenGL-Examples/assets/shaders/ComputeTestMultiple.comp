#version 430
layout (local_size_x = 1) in;

layout(std430, binding = 0) buffer MultipleArrayBuffer {
	float floatArr[5];
	int intArr[5];
};

void main(){
	uint indx = gl_GlobalInvocationID.x;
	
	floatArr[indx] += 0.5f;
	intArr[indx] += 1;
}
